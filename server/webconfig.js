let webconfig = {
  clientAddress: 'http://localhost:8000',
  privateKey: 'tocotoco',
  dbPoolConfig: {
    connectionLimit: 20,
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'digital_signage'
  }
}

module.exports = webconfig;